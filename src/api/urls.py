from django.urls import path

from . import views

urlpatterns = [
    # path('my-contacts/', views.MyContactsViewSet.as_view({'get': 'list'}), name='my_contacts'),
    # path('my-prospects/', views.MyProspectsViewSet.as_view({'get': 'list'}), name='my_prospects'),
    # path('my-clients/', views.MyClientsViewSet.as_view({'get': 'list'}), name='my_clients'),
    # path('my-contracted-clients/', views.MyContractedClientsViewSet.as_view({'get': 'list'}), name='my_contracted_clients'),
]